version: "3.9"

services:
    server:
        container_name: prakticum-server
        image: prackicum-server
        build:
            context: .
            dockerfile: Dockerfile.server
            args:
              SERVER_PORT: ${SERVER_PORT}
        restart: always
        ports:
            - "${SERVER_PORT}:${SERVER_PORT}"
        environment:
          SERVER_PORT: ${SERVER_PORT}
        depends_on:
          - postgres
        volumes:
          - ./.env:/app/.env
          - ./packages/server/src/modules/prisma:/app/prisma
        command: ["sh", "-c", "npx prisma migrate deploy && npx prisma generate && node /app/server/src/index.js"]
        networks:
          - awesome

    postgres:
      image: postgres:14     
      ports:
        - "${POSTGRES_PORT}:${POSTGRES_PORT}"
      environment:
        POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
        POSTGRES_USER: ${POSTGRES_USER}
        POSTGRES_DB: ${POSTGRES_DB}
      volumes:
        - ./tmp/pgdata:/var/lib/postgresql/data
      networks:
        - awesome

networks:
  awesome:
    driver: bridge
